# Copy to .env and fill in to enable LIVE mode (Claude-powered agents).
# Without these, the system runs in deterministic MOCK mode and still works end-to-end.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
PORT=8000
